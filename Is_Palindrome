class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        
        number = str(x)
        rev = number[::-1]
        if rev == number:
            return True
        else:
            return False    
